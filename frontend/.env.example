# API Base URL
# Leave empty or omit during local development to use Vite's proxy configured in vite.config.ts
# Set in production (or on Vercel dashboard) to your deployed backend URL:
VITE_API_BASE_URL=https://resume-to-jd-gap-analyzer.onrender.com
